version: 2

models:
  - name: time_spine_daily
    description: "Daily time spine for MetricFlow and semantic layer time-based joins."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Daily grain date used by MetricFlow as the standard time spine join column."
        granularity: day
